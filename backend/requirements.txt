# mayavius backend — lightweight runtime deps (NO ML; see requirements-ml.txt).
# Pinned to the versions installed during scaffolding (Python 3.10, June 2026).
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32
pydantic==2.13.4
pydantic-settings==2.14.1
